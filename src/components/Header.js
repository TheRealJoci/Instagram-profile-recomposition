import React from 'react'
import UploadForm from './UploadForm'

const Header = () => {
    return (
        <nav>
            <div className="header">
                Vista
                <UploadForm />
            </div>
        </nav>
    )
}

export default Header
